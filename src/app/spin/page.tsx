import SpinPageContent from '@/components/spin/SpinPageContent';

export const metadata = {
  title: 'Spin & Win — BEST STORE',
  description: 'Spin the daily prize wheel for free rewards.',
};

export default function SpinPage() {
  return <SpinPageContent />;
}
